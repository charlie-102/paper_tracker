# Paper Tracker Environment Variables
# ====================================
# Copy this file to .env and fill in the values
# cp .env.example .env

# GitHub Personal Access Token (recommended for higher rate limits)
# Without token: 60 requests/hour
# With token: 5000 requests/hour
# Create at: https://github.com/settings/tokens
# Required scopes: public_repo (for public repos only)
GITHUB_TOKEN=

# Override minimum stars filter (default: 10 from config.yaml)
# TRACKER_MIN_STARS=20

# Override year filter (default: 2024 from config.yaml)
# TRACKER_YEAR=2024
